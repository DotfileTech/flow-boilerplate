- country should not default to Australia if undefined
- default country to locale browser
- manage countries selector with global setting
- dynamic choice of data from individual to display in listing